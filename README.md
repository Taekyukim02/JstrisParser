# JstrisParser

Python program to parse Jstris games.

## Required Packages:
* numpy
* pandas:
* pynput:
  * `pip install pynput`
  * `pip install --upgrade pyobjc==7.3`
* python-mss:
  * `python -m pip install -U --user mss`